<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PokemonCardCollection.ViewModels"
             x:Class="PokemonCardCollection.Views.ListPage"
             x:DataType="vm:ListViewModel"
             Title="My Collection">

    <Grid RowDefinitions="Auto,*" Padding="16">

        <!-- Header with Add button -->
        <HorizontalStackLayout Grid.Row="0" HorizontalOptions="End" Spacing="8" Margin="0,0,0,12">
            <Button Text="＋ Add Card"
                    Command="{Binding GoToAddCardCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="8"
                    Padding="16,8" />
        </HorizontalStackLayout>

        <!-- Card list -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Cards}"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding GoToDetailCommand}"
                        SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="8">
                    <Label Text="No cards yet!" FontSize="20" HorizontalTextAlignment="Center" />
                    <Label Text="Tap + Add Card to start your collection." FontSize="14" TextColor="Gray" HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:Object">
                    <Frame Margin="0,6" Padding="12" CornerRadius="12" HasShadow="True" BorderColor="LightGray">
                        <Grid ColumnDefinitions="80,*" ColumnSpacing="12">
                            <!-- Card thumbnail -->
                            <Image Grid.Column="0"
                                   Source="{Binding ImageUri}"
                                   Aspect="AspectFill"
                                   HeightRequest="80"
                                   WidthRequest="80" />

                            <!-- Card info -->
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding Category}" FontSize="13" TextColor="Gray" />
                                    <Label Text="•" FontSize="13" TextColor="Gray" />
                                    <Label Text="{Binding Rarity}" FontSize="13" TextColor="Gray" />
                                </HorizontalStackLayout>
                                <Label FontSize="14" TextColor="Green">
                                    <Label.Text>
                                        <Binding Path="EstimatedValue" StringFormat="$ {0:N2}" />
                                    </Label.Text>
                                </Label>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</ContentPage>
